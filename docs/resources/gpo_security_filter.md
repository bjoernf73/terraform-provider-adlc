---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "adlc_gpo_security_filter Resource - adlc"
subcategory: ""
description: |-
  Authoritatively manages the principals allowed to apply one GPO. It removes GpoApply from every other explicit trustee, while keeping Authenticated Users at GpoRead so the policy remains readable. It does not modify edit or owner permissions.
---

# adlc_gpo_security_filter (Resource)

Authoritatively manages the principals allowed to apply one GPO. It removes `GpoApply` from every other explicit trustee, while keeping `Authenticated Users` at `GpoRead` so the policy remains readable. It does not modify edit or owner permissions.

## Example Usage

```terraform
resource "adlc_json_gpo" "server_baseline" {
  path        = "${path.module}/json_gpo/Servers Baseline.json"
  target_name = "Servers Baseline"
}

resource "adlc_group" "server_computers" {
  name = "Server Computers"
  path = "Groups"
}

# Only members of this group can apply the GPO. Authenticated Users retains GpoRead.
resource "adlc_gpo_security_filter" "server_baseline" {
  gpo        = adlc_json_gpo.server_baseline.target_name
  principals = [adlc_group.server_computers.distinguished_name]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `gpo` (String) GPO by GUID or display name.
- `principals` (Set of String) Complete set of users, groups or computers allowed to apply the GPO. Each receives `GpoApply` (read plus apply).

### Read-Only

- `gpo_dn` (String) Distinguished name of the GPO container.
- `id` (String) Resolved GUID of `gpo`.
- `principal_sids` (Set of String) Resolved SIDs of the current `GpoApply` trustees.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Security filters are imported with the GPO's GUID or display name.
terraform import adlc_gpo_security_filter.server_baseline "Servers Baseline"
```
